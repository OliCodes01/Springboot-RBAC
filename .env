# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=jwt_app_db
DB_USER=postgres
DB_PASSWORD=admin

# Server Configuration
SERVER_PORT=8080

# JWT Configuration
# Keys are now stored as PEM files in src/main/resources/keys/
# - private_key.pem: RSA private key for signing tokens
# - public_key.pem: RSA public key for verifying tokens
